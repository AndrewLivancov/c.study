#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a;
    for (a = 2; a <= 18; a+=2)
        printf("%d\n", a);

        a = 2;

            do
        {
            printf(" %d\n", a);
            a += 2;
        }
         while (a <= 18);
    return 0;
}
